name: code_scout_http_example
description: Wiring the CodeScout http client wrapper into an app.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.10.0
  flutter: '>=3.38.0'

dependencies:
  flutter:
    sdk: flutter
  code_scout: ^1.5.0
  code_scout_http:
    path: ../
  http: '>=1.0.0 <3.0.0'

dev_dependencies:
  flutter_lints: ^6.0.0
